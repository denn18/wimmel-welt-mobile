# Copy this file to backend/.env and adjust the values to match your MongoDB setup.
#
# Option 1: Provide a complete connection string. This takes precedence over the
# granular options below.
# MONGODB_URI="mongodb+srv://dennieentwicklung_db_user:fTtZX9SCxoaz9xnn@<cluster-host>/dennieentwicklung_db?retryWrites=true&w=majority&appName=<cluster-name>"
#
# Option 2: Configure the individual parts. This is convenient for managed
# hosting providers (Render, Railway, etc.) where credentials are stored as
# separate environment variables.
MONGODB_PROTOCOL=mongodb+srv
MONGODB_HOST=<cluster-host>
MONGODB_USERNAME=dennieentwicklung_db_user
MONGODB_PASSWORD=fTtZX9SCxoaz9xnn
MONGODB_DB_NAME=dennieentwicklung_db
# MONGODB_PORT=27017            # Only used for mongodb:// URIs
# MONGODB_AUTH_SOURCE=admin     # Add if your cluster requires a specific auth DB
# MONGODB_OPTIONS=retryWrites=true&w=majority&appName=<cluster-name>

# Application port (optional, default: 5000)
# PORT=5000

# File storage (defaults to S3). Set FILE_STORAGE_MODE=local for local filesystem storage.
FILE_STORAGE_MODE=s3
# AWS_S3_BUCKET=<your-bucket-name>
# AWS_REGION=eu-north-1
# AWS_ACCESS_KEY_ID=<your-access-key>
# AWS_SECRET_ACCESS_KEY=<your-secret-key>
