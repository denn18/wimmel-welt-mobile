# Base URLs for different environments. Set one of these as EXPO_PUBLIC_API_BASE_URL
# or override directly in your local .env file.
EXPO_PUBLIC_API_BASE_URL_DEV=http://localhost:2000
EXPO_PUBLIC_API_BASE_URL_STAGE=https://stage-api.example.com
EXPO_PUBLIC_API_BASE_URL_PROD=https://api.example.com

# Active base URL used by the app. Copy the appropriate value from above or provide
# a custom endpoint when connecting to a deployed backend.
EXPO_PUBLIC_API_BASE_URL=
